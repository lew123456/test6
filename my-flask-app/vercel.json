{
  "builds": [
    {
      "src": "Dockerfile",
      "use": "@vercel/docker"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/" // This is not necessary for Docker deployments, but you can keep it for clarity.
    }
  ]
}